{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Page d'accueil. ✅</h1>
    <h2> {{ pluralize(articles|length, 'article','articles') }}  </h2>
    
  

    {% for article in articles %}

    <div class="md:flex">
  <div class="md:flex-shrink-0">
  {#
  |
    a continuer : filter les images pour avoir une taille standart imagine_filter('carre') 
  #}
  <img class="rounded-lg md:w-56" src="{{ vich_uploader_asset(article )  
                                           }}" alt="{{ article.titre }}" />
  </div>
  <div class="mt-4 md:mt-0 md:ml-6">
    <div class="uppercase tracking-wide text-sm text-indigo-600 font-bold">
    <a href="{{path('post', {idPost:article.id}) }}">{{article.titre}} <a/>
    </div>
    <p class="mt-2 text-gray-600">{{article.description}}</p>
  </div>
</div>
{% endfor %}
</div>
{% endblock %}

